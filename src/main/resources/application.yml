zuul:
  routes:
    greetings:
      path: /wynats-configuration-api/**
      serviceId: wynats-configuration-api
      stripPrefix: false
      
# Discovery Server Access
eureka:
  client:
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
 
server:
  port: 8080   # HTTP (Tomcat) port
      